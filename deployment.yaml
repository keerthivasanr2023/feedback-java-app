apiVersion: apps/v1
kind: Deployment
metadata:
  name: Feedback-java-app-deployment
  labels:
    app: Feedback-java-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Feedback-java-app
  template:
    metadata:
      labels:
        app: Feedback-java-app
    spec:
      containers:
        - name: Feedback-java-app
          image: keerthivasan08/Feedback-java-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"

          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10

          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: Feedback-java-app-service
spec:
  type: NodePort
  selector:
    app: Feedback-java-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001